/* Requires the Docker Pipeline plugin */
pipeline {
    agent {
        docker {
            alwaysPull true
            image 'node:16.16.0-alpine'
            reuseNode true
        }
     }
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
    }
}
