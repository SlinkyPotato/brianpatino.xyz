/* Requires the Docker Pipeline plugin */
pipeline {
    agent {
        docker {
            image 'node:18.16.0-alpine'
            reuseNode true
        } 
     }
    stages {
        stage('build') {
            steps {
                sh 'node --version'
            }
        }
    }
}
